{
  "name": "round-robin-api",
  "version": "1.0.0",
  "description": "This repository is for showing the implementation of API load balancing using Round Robin algorithm.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start:main": "npm run build && node dist/server/main/index.js",
    "start:load-balancer": "npm run build && node dist/server/load-balancer/index.js",
    "test": "export TEST_MODE=true && jest --verbose tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^29.5.12",
    "axios": "^1.6.8",
    "body-parser": "^1.20.2",
    "express": "^4.19.2",
    "jest": "^29.7.0",
    "jest-extended": "^4.0.2",
    "jest-mock-extended": "^3.0.6",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5",
    "winston": "^3.13.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node": "^10.9.2"
  }
}
